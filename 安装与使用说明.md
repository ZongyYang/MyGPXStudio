# MyGPXStudio 安装与使用说明

## 系统要求

- Apple Silicon Mac（M1、M2、M3、M4 等）
- macOS 14 或更高版本
- 国内地点需要可访问高德 Web 服务 API 的网络环境；海外地点可使用 Apple MapKit

## 安装

1. 解压收到的压缩包。
2. 将 `MyGPXStudio.app` 拖入“应用程序”文件夹。
3. 第一次打开时，如系统提示无法验证开发者，请在 App 图标上右键选择“打开”；仍被阻止时，到“系统设置 → 隐私与安全性”选择“仍要打开”。
4. 打开 App 后按 `⌘,`，填写自己的高德 Web 服务 API Key；安全密钥可选。海外地点可以直接通过 Apple 地图搜索和规划，不依赖高德 Key。

API Key 只保存在该 Mac 的系统钥匙串中，不会包含在 App 或本压缩包内。

## 基本使用

1. 设置起点、终点；需要时添加途经点并调整顺序。
2. 选择驾车、骑行或步行，设置出发日期和 24 小时时间。
3. 点击“生成路线”，可从竖向路线建议中选择方案。
4. 点击“导出 GPX”。保存面板默认打开到 `下载/GPX Output/生成路线时的出发日期` 文件夹，例如 `下载/GPX Output/2023-07-25`；如果文件夹不存在，应用会自动创建。也可以在保存面板中改选其他位置，应用会在所选父目录下创建该出发日期文件夹。

如果起点和终点相同，请至少添加一个途经点；应用会规划经过途经点后回到起点的路线。若“生成路线”按钮不可点击，按钮下方会直接说明是起点、终点、哪一个途经点未完成，还是缺少高德 API Key。

同一日期文件夹内遇到同名 GPX 时，应用会自动添加 `-2`、`-3` 等序号，避免覆盖原文件。

## 说明

此版本使用本机临时签名，未进行 Apple 公证，因此首次打开会有系统安全提示。安装包内附带 `MyGPXStudio-开发记录.md`，其中包含功能记录、源码索引和后续开发说明；最新可编译源码以项目文件为准。
